# Next.js на Netlify
# publish = ".next" переопределяет настройку UI (нельзя оставлять корень репозитория)
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
